import shutil

def centralize_images(image_paths, destination_folder):
    for image_path in image_paths:
        # Aqui você pode implementar a cópia das imagens para a pasta de destino
        # Centralize as imagens que precisam de validação humana

if __name__ == "__main__":
    # Coloque aqui o código para ler as imagens que precisam de centralização
    # e definir a pasta de destino

    image_paths = ["anomaly1.png", "anomaly2.png"]
    destination_folder = "centralized_images"

    centralize_images(image_paths, destination_folder)
